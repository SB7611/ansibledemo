---
- name: First pull demo
  hosts: localhost
  tasks:
    - name: print hello world
      debug: 
        msg: "Hello"
    - name: print host name
      debug: 
        var: ansible_hostname
    - name: print Ubantu version
      debug:
        var: ansible_distribution
